// All interfaces are exported from types.ts for better cohesion
export type {
    ISyncApiClient,
    ISyncConfig,
    ISyncState,
    IFileSystemScanner,
    IFileOps,
    IKeepAwake,
    IUserPrompt,
} from './types';
